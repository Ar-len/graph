package com.ktwl.graph;

import javax.sound.midi.Soundbank;
import java.util.*;
import java.util.logging.XMLFormatter;
import java.util.stream.Stream;

import static jdk.nashorn.internal.objects.NativeMath.random;

/**
 * @author yuxingxing
 * @date 2020/5/21 17:02
 */
public class Demo {
	public static void main(String[] args) {
		Stream<String> stream = Stream.of("12356", "2", "3");
//		stream.map(n->n.concat(".txt")).forEach(System.out::println);
//		stream.filter(n->n.contains("2")).forEach(System.out::println);
//		stream.flatMap(n->Stream.of(n.split(""))).forEach(System.out::println);
		
	
	}
}
